# Run `npm install` in the project directory to install project packeges.

## Then run `npm start` - runs devserver.

### Open [http://localhost:3000](http://localhost:3000) to view result in the browser.